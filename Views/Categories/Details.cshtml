<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
    <link rel="stylesheet" type="text/css" href="~/css/styles.css">
    <title></title>
  </head>
  <body>
    <div class="container">
      <h3>Here are all the items in this category:</h3>
      <hr class="style-two">
      <ol>
        @foreach (var item in @Model["items"])
        {
          <li><a href='/category/@Model["category"].GetId()/items/@item.GetId()'>@item.GetDescription()</a></li>
        }
      </ol>
      <hr class="style-two">
      <form class="inline" action='/categories/@Model["category"].GetId()/items/new' method="get">
        <button class="btn btn-info"type="submit" name="button">Add an Item</button>
      </form>
      <form class="inline" action='/categories' method="get">
        <button class="btn btn-info"type="submit" name="button">Return to Categories</button>
      </form>
      <form class="inline" action='/' method="get">
        <button class="btn btn-info"type="submit" name="button">Return to Home</button>
      </form>
      <br>
      <br>
      <form action="/items/delete" method="post">
        <button class="btn btn-danger"type="submit" name="button">Clear All Items</button>
      </form>
    </div>
  </body>
</html>
